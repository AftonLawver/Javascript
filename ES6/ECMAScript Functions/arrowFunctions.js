// arrow functions are a new function syntax that is cleaner

const milesRan = (runs) => {
  console.log(runs);
};

milesRan([1.2,3.01, 0.5]);

let fruits = ["apple", "banana", "orange"];

fruits.map( (fruit) => {
  console.log(fruit);
});
